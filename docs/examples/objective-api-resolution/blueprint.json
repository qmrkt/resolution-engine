{
  "id": "objective-api-resolution",
  "name": "Objective API Resolution",
  "description": "Resolve from one canonical API response and cancel if it does not produce a valid outcome.",
  "version": 1,
  "nodes": [
    {
      "id": "fetch",
      "type": "api_fetch",
      "label": "Fetch Canonical Source",
      "config": {
        "url": "https://api.example.com/markets/{{inputs.market.id}}/resolution",
        "method": "GET",
        "json_path": "data.outcome",
        "outcome_mapping": {
          "YES": "0",
          "NO": "1"
        },
        "timeout_seconds": 30
      }
    },
    {
      "id": "submit",
      "type": "return",
      "label": "Submit Outcome",
      "config": {
        "value": {
          "status": "success",
          "outcome": "{{results.fetch.outcome}}"
        }
      }
    },
    {
      "id": "cancel",
      "type": "return",
      "label": "Cancel",
      "config": {
        "value": {
          "status": "cancelled",
          "reason": "The canonical API did not produce a valid mapped outcome."
        }
      }
    }
  ],
  "edges": [
    {
      "from": "fetch",
      "to": "submit",
      "condition": "results.fetch.status == 'success' && results.fetch.outcome != ''"
    },
    {
      "from": "fetch",
      "to": "cancel",
      "condition": "results.fetch.status != 'success' || results.fetch.outcome == ''"
    }
  ],
  "budget": {
    "max_total_time_seconds": 1800
  }
}
